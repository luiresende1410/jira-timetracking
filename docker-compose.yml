services:
  backend:
    build: ./backend
    container_name: timetracking-backend
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/colaboradores.json:/app/colaboradores.json
      - ./backend/clientes_msp.json:/app/clientes_msp.json
      - ./backend/perfis_capacity.json:/app/perfis_capacity.json
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: timetracking-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

